databaseChangeLog:
  - changeSet:
      id: create-table-user
      author: dbarsukov
      preConditions:
        - onFail: MARK_RAN
          not:
            tableExists:
              tableName: users
      changes:
        - createTable:
            columns:
              - column:
                  autoIncrement: true
                  constraints:
                    nullable: false
                    primaryKey: true
                    primaryKeyName: user_pkey
                  name: id
                  type: BIGINT
              - column:
                  constraints:
                    nullable: false
                  name: password
                  type: ${textColumnType}
              - column:
                  constraints:
                    nullable: false
                  name: first_name
                  type: ${textColumnType}
              - column:
                  name: last_name
                  type: ${textColumnType}
            tableName: users